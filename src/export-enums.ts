export enum SoundType {
  ONCE,
  BACKGROUND,
}

export enum LayoutMode {
  LANDSCAPE,
  PORTRAIT,
}

export enum JoystickActionType {
  START = "start",
  MOVE = "move",
  STOP = "stop",
}

export enum JoystickDirection {
  FORWARD = "FORWARD",
  RIGHT = "RIGHT",
  LEFT = "LEFT",
  BACKWARD = "BACKWARD",
}

export enum SoundStoreKey {
  ONCE = "sound-once",
  BACKGROUND = "sound-background",
}
